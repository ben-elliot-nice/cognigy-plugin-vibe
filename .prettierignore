dist
node_modules
.claude/skills/
# Skill markdown is hand-authored content; keep it out of Prettier's reflow.
plugin/skills/
# design-forge is a hand-authored, self-contained spec plugin (skills + reference
# docs); its fenced YAML/prose is intentionally hand-formatted, so keep it all
# out of Prettier's reflow, same rationale as plugin/skills/ above.
design-forge/
